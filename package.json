{
  "parivate": true,
  "name": "jest-gauge",
  "version": "0.0.1",
  "description": "Write Gauge-like acceptance tests with Jest-based steps",
  "main": "dist/index.js",
  "scripts": {
    "test": "npx jest",
    "build": "npx esbuild src/index.ts --bundle --outfile=dist/index.js --platform=node"
  },
  "author": "@tnzk",
  "license": "MIT",
  "dependencies": {
    "markdown-it": "^12.0.6",
    "markdown-it-ast": "0.0.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "esbuild": "^0.12.1",
    "esbuild-jest": "^0.5.0",
    "jest": "^26.6.3"
  }
}
